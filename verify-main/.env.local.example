# ============================================
# verify-main Environment Variables
# System Status Dashboard (Port 80)
# Next.js 15 Frontend Application
# ============================================

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
PORT=80
HOSTNAME=0.0.0.0

# ============================================
# API Configuration (Required)
# ============================================
# Backend API URL (verify-monitor-api)
NEXT_PUBLIC_API_URL=http://localhost:3001

# WebSocket URL (Socket.IO)
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# 프로덕션에서는 실제 도메인 사용:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_WS_URL=wss://api.yourdomain.com

# ============================================
# Application Settings
# ============================================
NEXT_PUBLIC_APP_NAME=Status Monitor
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_COMPANY_NAME=Argos Identity

# ============================================
# Feature Flags
# ============================================
NEXT_PUBLIC_ENABLE_WEBSOCKET=true
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true
NEXT_PUBLIC_ENABLE_DARK_MODE=false
NEXT_PUBLIC_AUTO_REFRESH=true
NEXT_PUBLIC_REFRESH_INTERVAL=60000

# ============================================
# Display Settings
# ============================================
NEXT_PUBLIC_UPTIME_DAYS=90
# 표시할 가동률 기간 (일)

NEXT_PUBLIC_MAX_INCIDENTS=10
# Past Incidents에 표시할 최대 인시던트 수

NEXT_PUBLIC_DATE_FORMAT=YYYY-MM-DD HH:mm:ss
# 날짜 형식

NEXT_PUBLIC_TIMEZONE=Asia/Seoul
# 시간대

# ============================================
# Internationalization (i18n)
# ============================================
NEXT_PUBLIC_DEFAULT_LOCALE=ko
NEXT_PUBLIC_SUPPORTED_LOCALES=ko,en
# 지원 언어: 한국어, 영어

# ============================================
# Analytics (Optional)
# ============================================
# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry (Error Tracking)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# ============================================
# Performance Optimization
# ============================================
NEXT_PUBLIC_ENABLE_SERVICE_WORKER=false
NEXT_PUBLIC_ENABLE_PWA=false
NEXT_PUBLIC_IMAGE_OPTIMIZATION=true

# ============================================
# Security Settings
# ============================================
NEXT_PUBLIC_CSP_ENABLED=false
# Content Security Policy

# ============================================
# Theme Configuration
# ============================================
NEXT_PUBLIC_THEME=light
NEXT_PUBLIC_PRIMARY_COLOR=#8BC34A
# Green - Operational status color

# ============================================
# Socket.IO Client Configuration
# ============================================
NEXT_PUBLIC_SOCKET_RECONNECTION=true
NEXT_PUBLIC_SOCKET_RECONNECTION_ATTEMPTS=5
NEXT_PUBLIC_SOCKET_RECONNECTION_DELAY=1000
NEXT_PUBLIC_SOCKET_TIMEOUT=20000

# ============================================
# API Request Configuration
# ============================================
NEXT_PUBLIC_API_TIMEOUT=10000
# API 요청 타임아웃 (밀리초)

NEXT_PUBLIC_RETRY_ATTEMPTS=3
# 실패 시 재시도 횟수

NEXT_PUBLIC_RETRY_DELAY=1000
# 재시도 간격 (밀리초)

# ============================================
# Cache Configuration
# ============================================
NEXT_PUBLIC_CACHE_ENABLED=true
NEXT_PUBLIC_CACHE_TTL=60
# 캐시 유효 시간 (초)

# ============================================
# Development Settings
# ============================================
# NEXT_PUBLIC_DEBUG=false
# NEXT_PUBLIC_MOCK_DATA=false
# NEXT_PUBLIC_SHOW_DEV_TOOLS=false

# ============================================
# Build Configuration
# ============================================
# Next.js 빌드 설정은 next.config.ts에서 관리
# 여기는 런타임 환경변수만 정의

# ============================================
# 주의사항
# ============================================
# 1. NEXT_PUBLIC_ 접두사가 붙은 변수만 브라우저에 노출됨
# 2. 민감한 정보(API 키, 비밀번호)는 NEXT_PUBLIC_ 사용 금지
# 3. 빌드 시점에 환경변수가 번들에 포함됨
# 4. 런타임에 환경변수 변경 시 재빌드 필요

# ============================================
# Port 80 바인딩 권한 설정
# ============================================
# Port 80은 root 권한 필요. 다음 중 하나 선택:
#
# 옵션 1: Node.js에 권한 부여 (권장)
#   sudo setcap 'cap_net_bind_service=+ep' $(which node)
#
# 옵션 2: 다른 포트 사용 + Nginx/HAProxy 프록시
#   PORT=3000 으로 변경
#   Nginx에서 80 → 3000 프록시 설정
#
# 옵션 3: sudo로 PM2 실행 (비권장)
#   sudo pm2 start ecosystem.config.js

# ============================================
# 설정 완료 후 작업
# ============================================
# 1. NEXT_PUBLIC_API_URL을 실제 API 서버 주소로 변경
# 2. NEXT_PUBLIC_WS_URL을 실제 WebSocket 주소로 변경
# 3. Port 80 권한 설정 (위 옵션 참고)
# 4. npm run build 실행
# 5. PM2로 실행: pm2 start ecosystem.config.js --only verify-main
